export const login = async (email: string, password: string) => {};

export const register = (
  phoneNumber: string,
  username: string,
  phoneCode: string,
  password: string,
) => {};

// auth.sendPhoneCode(phoneNumber).then((res) => {

export const fetchCurrentUser = async (token: string) => {};
